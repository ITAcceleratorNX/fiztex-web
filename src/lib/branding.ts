export const APP_NAME = 'PhysTech';

// Used for the browser tab title in the admin UI.
export const ADMIN_TITLE = `${APP_NAME} — Административная панель`;

/**
 * Заголовок вкладки для публичных страниц. Главная теперь открыта всем, и
 * «Административная панель» в заголовке вкладки и в закладках поступающего
 * выглядела бы ошибкой.
 */
export const PUBLIC_TITLE = `${APP_NAME} — Вступительные тесты`;

